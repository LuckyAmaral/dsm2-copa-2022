import 'package:flutter/material.dart';

class ListaGrupos extends StatelessWidget {
  const ListaGrupos({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Lista de Grupos"));
  }
}
